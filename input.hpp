#ifndef INPUT_HPP
#define INPUT_HPP

class Input {
    public:
        enum KeyPress {
            DEFAULT,
            UP,
            DOWN,
            LEFT,
            RIGHT,
            TOTAL
        };
};

#endif